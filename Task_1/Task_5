employees = [
    ("Ajit", 50000),
    ("Rahul", 75000),
    ("Aman", 60000)
]

sorted_emp = sorted(employees, key=lambda emp: emp[1])

print(sorted_emp)



students = [
    ("Ajit", 85),
    ("Rahul", 35),
    ("Aman", 72),
    ("Karan", 28)
]

passed = list(filter(lambda x: x[1] >= 40, students))

print(passed)



names = ["ajit", "rahul", "aman"]

upper_names = list(map(lambda name: name.upper(), names))

print(upper_names)



# Default Parameter Value
# Problem 5:- Write a function that greets a user. If no name is provided, it should greet with a default name.

def greet(name = 'Ajay'):
  return "Hello, " + name + "!"

print(greet('ajay'))


# Lambda Function
# Problem 6:- Create a lambda function to compute the cube of a number.

cube = lambda x: x ** 3
print(cube(3))

# Function with *args
# Problem 7:- Write a function that takes variable number of arguments and returns their sum.

def sum_all(*args):
  return sum(args)

print(sum_all(1,2,3))
print(sum_all(1,2,3,4,5))


# Function with **Kwargs
# Problem 8:- Create a function that accepts any number of keyword arguments and prints them in the format key: value.

def print_kwargs(**kwargs):
  for key, value in kwargs.items():
    print(f"{key}: {value}")
print_kwargs(name = "Ajit", age = 22, city = "Gurugram")

# Generate Function with yield
# Problem 9:- Write a generator function that yields even numbers up to a specified limit.


def even_numbers(limit):
  for i in range(2, limit + 1, 2):
    yield i
for num in even_numbers(10):
  print(num)
